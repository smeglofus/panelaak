{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability for the main game window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
